name: .NET CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 7.x

      - name: Restore dependencies
        run: dotnet restore GeoCalculator.sln

      - name: Build
        run: dotnet build GeoCalculator.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test GeoCalculator.sln --no-build --configuration Release --verbosity normal
